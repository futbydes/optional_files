:set number
:syntax on
colorscheme pablo

" --- history / filehandling --- 

set history=1000
set undolevels=1000
set autoread "reload files if changed externally

" --- backup / swapfiles ---

set nobackup
set nowritebackup
set noswapfile
set noeb vb t_vb= "no errbeps,visbeeps erms flashes"
